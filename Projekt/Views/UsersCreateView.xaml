<UserControl x:Class="Projekt.Views.UsersCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views"
             xmlns:viewmodels="clr-namespace:Projekt.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type viewmodels:UsersCreateViewModel}, IsDesignTimeCreatable=True}"
             >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            
        </Grid.ColumnDefinitions>
  
            <Label>Imię: </Label>
            <TextBox Grid.Column="1" Text="{Binding FirstName}"/>
       
        
            <Label Grid.Row="1">Nazwisko:</Label>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LastName}"/>


        <Label Grid.Row="2">Hasło:</Label>
        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <PasswordBox/>
            <Button Grid.Column="1" HorizontalAlignment="Right" Padding="10,0,10,0">Losuj</Button>
        </Grid>

        <Label Grid.Row="3">Poziom uprawnień:</Label>
        <ComboBox Grid.Row="3" Grid.Column="1" 
                  SelectedItem="{Binding SelectedUserLevel, Mode=TwoWay}" 
                  ItemsSource="{Binding Roles}" 
                  DisplayMemberPath="Name" 
                  SelectedValuePath="Id"/>






    </Grid>
</UserControl>
