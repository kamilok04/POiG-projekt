<UserControl x:Class="Projekt.Views.FacultyEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid Grid.ColumnSpan="4" ItemsSource="{Binding Faculties}"/>
        <Button Grid.Row="1" Command="{Binding DeleteSelectedCommand}"></Button>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding UpdateSelectedCommand}"></Button>
        <Button Grid.Row="1" Grid.Column="2" Command="{Binding CreateCommand}"></Button>
    </Grid>
</UserControl>
