<UserControl x:Class="Projekt.Views.CurrentProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
                xmlns:viewmodels="clr-namespace:Projekt.ViewModels"
             d:DataContext="{d:DesignInstance Type={x:Type viewmodels:CurrentProfileViewModel}, IsDesignTimeCreatable=True}"
             >

 
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Grid.ColumnSpan="2" HorizontalAlignment="Stretch">Obecny profil</Label>
        <Label Grid.Row="1" >Imię:</Label><Label Grid.Row="1" Grid.Column="1"><Binding Path="Name"></Binding></Label>
        <Label Grid.Row="2" >Nazwisko:</Label><Label Grid.Row="2" Grid.Column="1"><Binding Path="Surname"></Binding></Label>
        <Label Grid.Row="3" >Data urodzenia:</Label><Label Grid.Row="3" Grid.Column="1"><Binding Path="BirthDate"></Binding></Label>
        <Label Grid.Row="4" >Adres e-mail:</Label><Label Grid.Row="4" Grid.Column="1"><Binding Path="EMail"></Binding></Label>
            
    </Grid>
        
  
</UserControl>
